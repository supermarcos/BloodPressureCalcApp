import 'package:flutter/material.dart';

class DayTimePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('daytime screen')),
    );
  }
}
