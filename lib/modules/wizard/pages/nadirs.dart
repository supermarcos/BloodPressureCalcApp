import 'package:flutter/material.dart';

class NadirsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('nadirs screen')),
    );
  }
}
